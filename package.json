{
  "name": "marzy-site",
  "version": "0.1.0",
  "private": true,
  "description": "Marzy marketing site — pure HTML/CSS/JS, no build step.",
  "scripts": {
    "dev": "python3 -m http.server 8000",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.97.0"
  }
}
